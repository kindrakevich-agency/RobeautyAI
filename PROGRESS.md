# Прогрес

## M0 — скрапер каталогу

**Зроблено.** Два джерела, злиття по SKU:

- Store API: робочий шард `store.tildaapi.one` (класичний хост відповідає
  `{"redirectto":"one"}`). Два сторпарти: каталог (104 позиції) + бестселери
  (14, підмножина). `editions` дають SKU/ціну/фото і структуровані атрибути
  («Проблема:», «Зона нанесення:») прямо з API.
- Лендінги: sitemap 306 URL → 297 контентних сторінок у кеші `data/raw/`.
  Ціни й SKU — з `#order:`-лінків, закреслені суми (U+0336) → old_price.

**Підсумок:** products = 179 (135 API+лендінг, 44 тільки лендінг — сети й
комплекти), pages = 153, у 45 позицій — окрема upsell-ціна.

**Важливе рішення:** той самий SKU живе в апсел-блоках чужих лендінгів і на
головній з промо-ціною. Ціну товару переписує тільки його ВЛАСНИЙ лендінг;
згадки деінде йдуть у `upsell_price`. Реальних розбіжностей API↔лендінг — 5
(промо на сторінках), взято лендінгову.

**Далі:** M1 міграції → завантаження каталогу в Postgres.
