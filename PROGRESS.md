# Прогрес

## M0 — скрапер каталогу

**Зроблено.** Два джерела, злиття по SKU:

- Store API: робочий шард `store.tildaapi.one` (класичний хост відповідає
  `{"redirectto":"one"}`). Два сторпарти: каталог (104 позиції) + бестселери
  (14, підмножина). `editions` дають SKU/ціну/фото і структуровані атрибути
  («Проблема:», «Зона нанесення:») прямо з API.
- Лендінги: sitemap 306 URL → 297 контентних сторінок у кеші `data/raw/`.
  Ціни й SKU — з `#order:`-лінків, закреслені суми (U+0336) → old_price.

**Підсумок:** products = 179 (135 API+лендінг, 44 тільки лендінг — сети й
комплекти), pages = 153, у 45 позицій — окрема upsell-ціна.

**Важливе рішення:** той самий SKU живе в апсел-блоках чужих лендінгів і на
головній з промо-ціною. Ціну товару переписує тільки його ВЛАСНИЙ лендінг;
згадки деінде йдуть у `upsell_price`. Реальних розбіжностей API↔лендінг — 5
(промо на сторінках), взято лендінгову.

**Далі:** M1 міграції → завантаження каталогу в Postgres.

## M1 — схема даних

**Зроблено.** 16 таблиць однією міграцією Alembic (`alembic upgrade head` на
чистій базі): каталог із i18n-шаром, чанки з вектором bge-m3 (1024) і двома
tsvector (uk/pl), розмови/повідомлення, тикети, синтетика для агентів
(orders, customers, shipments, sync_log), eval, unanswered і лічильник
витрат API (api_usage) для блоку Build vs Buy. Індекси: HNSW + два GIN.

Каталог завантажено: 179 products (135 API+лендінг, 44 сети з лендінгів),
153 pages, 177 позицій зі структурованими details (LLM-екстракція, 2 фейли
на нестандартній розмітці — у data/extract_errors.jsonl).

## M2 — польська локалізація

179 перекладів через LLM батчами по 4 паралельно: глосарій бренду (ARGIRELINE®,
bakuchiol не перекладаються), тон «наука + турбота», регуляторний фільтр ЄС.
Стоп-регекси спіймали 2 переклади з медичними твердженнями — лишились у draft
із приміткою, видно в адмінці. 20 позицій авто-approve, щоб чат польською
працював одразу.

## M3 — чанки та індексація

1224 чанки (1204 uk + 20 pl) через TEI bge-m3. Товар = один чанк із
розгорнутими details: активи, тип шкіри, проблеми, застосування, сумісність,
ціна, об'єм. Сторінки — по 1500 символів з перекриттям 200. tsvector обома
мовами через unaccent (діакритика PL).

## Агенти та API

Сім агентів + RAG-ядро, перевірені на живих даних:

| агент | результат прогону |
|---|---|
| 1. Підтвердження замовлень | 60 оброблено, 67% авто, 20 у чергу на дзвінок із причинами |
| 2. Невикупи | 35 персональних нагадувань, 87 240 ₴ під ризиком |
| 3. Аналіз діалогів | 10 розмов: тема, намір, тональність, результат |
| 4. Звернення | 15 згенеровано, 15 розібрано з чернетками відповідей |
| 5. 1С-синк | 3 конфлікти пояснені з рекомендацією дії |
| 7. SQL-аналітика | «скільки зависло у невикупах?» → 37 270 ₴ з видимим SQL |

**Мультиканальність.** Діалоги всіх каналів (веб, Instagram, Telegram, Viber)
лежать в одній таблиці — агент аналізу й адмінка працюють з ними однаково.
`customer_identities` зв'язує акаунти одного клієнта між каналами: 29 зв'язок
у демо, частина проставлена вручну, частина — за збігом телефону.

RAG перевірено: UK-питання дає картки з цінами, PL-питання відповідає
польською з українських джерел (один векторний простір bge-m3), медичне
питання ескалюється детерміновано, питання поза базою чесно каже «не знаю»
і пишеться в прогалини знань.
